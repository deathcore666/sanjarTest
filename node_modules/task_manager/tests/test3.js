const tm = require('../index');
const cons = require('../rd-kafka-consumer');

const foo = (err) => {
    if (err) {
        console.error(err)

    }else {
        console.log('ready');
        setInterval(() => {
            console.log('queue:', tm.tasksQueue)
        }, 2000);
    }
};

const bar = (msg) => {
    console.log('msg:', msg);
};

tm.init(foo, bar);


// cons.init(foo, bar);