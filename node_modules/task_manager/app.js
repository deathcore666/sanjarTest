const config = require('config');
const _ = require('lodash');

class TaskManagerClient {
    constructor(errCB, producer, consumer) {
        this.componentName = null;
        this.isPriority = null;
        this.updateTopic = null;

    }
}